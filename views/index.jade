extends layouts/default

block fb_meta
  meta(property='og:site_name', content='PartJS')
  meta(property='og:title', content='Next generation WoT and SPA.')
  meta(property='og:url', content='#{currentURL("")}')
  meta(property='og:image', content='')
  meta(property='article:published_time', content='')
  
block head
  title Next Generation WoT and SPA Framework

block neck

block feet
  script(src='/elements/build/bundle.js?#{cacheBreaker}')

block body

  section
    .container(style='margin-top: 30px; margin-bottom: 80px;')
      .row
        .col-md-8.col-md-offset-2.text-center
          h1(style='margin-bottom: 50px;') Next Generation WoT and SPA Framework
          h2(style='margin-bottom: 30px; font-weight: 300; line-height: 40px; text-align: left;') 
            span(style='font-weight: 300;') require('elements')
            br
            | .Spot( { template: _.template( $('#tmpl-spot-news').html() ) } )
            br
            | .add( { title: 'Hello', href: '/hello', img: 'hello.jpg' } );
            br
            | .add( { title: 'World', href: '/world', img: 'world.jpg' } );

  section
    .container(style='width: 80%;')
      .or-spacer
        .mask
          |   
          span
            i Components

  section
    .container(style='margin-top: 30px; margin-bottom: 80px;')
      .row
        .col-md-6.col-md-offset-3.text-center
          h2(style='margin-bottom: 30px;') Spot
          p A photo background square box that display a heading.
          pre
            | /* How to composite a Spot element. */
            | 
            | // Step 1: Require partjs elements.
            | var Elements = require('elements');
            | 
            | // Step 2: Create an element instance and bind data.
            | this.widget = new Elements.Spot({
            |   // backbone element
            |   el: this.$el, 
            |   // backbone data model
            |   model: app.SpotNews, 
            |   // backbone collection
            |   collection: app.SpotNewsCollection, 
            |   // underscore template
            |   template: this.template 
            | });
            | 
            | // Step 3: Add a new data model to element instance.
            | // * This will internally create the different model instances 
            | // * and composite UI.
            | this.widget.add({
            |   title: 'Coders', 
            |   href: '/coders',
            |   img: '//static.mokoversity.com/images/gallery/spot-course.jpg'
            | });
            | this.widget.add({
            |   title: 'Courses', 
            |   href: '/course/html5',
            |   img: '//static.mokoversity.com/images/gallery/spot-online.jpg'
            | });
            | this.widget.add({
            |   title: 'Training', 
            |   href: '/camp/full-stack',
            |   img: '//static.mokoversity.com/images/gallery/spot-training.jpg'
            | });
          a.ghost-button.ghost-button-inverse(href='#') View Demo
